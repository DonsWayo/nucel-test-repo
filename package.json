{
  "name": "nucel-test-repo",
  "version": "1.0.0",
  "description": "Sample project for testing Nucel workspaces",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "license": "MIT"
}
